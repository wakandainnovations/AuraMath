/**
 * Configuration for the Ask engine.
 *
 * <p>Holds {@link com.lit.fire.flame.nlq.config.AskEngineProperties} (bound from the
 * {@code aura.ask.*} prefix) and {@link com.lit.fire.flame.nlq.config.AskEngineConfiguration}
 * which registers it.
 */
package com.lit.fire.flame.nlq.config;
