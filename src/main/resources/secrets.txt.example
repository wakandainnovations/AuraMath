# Database Configuration
db.url=jdbc:postgresql://localhost:5432/aura
db.user=
db.password=

# Ask Engine LLM credentials (com.lit.fire.flame.nlq) — uncomment and set for your environment.
# Never commit a real key.
# anthropic.api.key=
